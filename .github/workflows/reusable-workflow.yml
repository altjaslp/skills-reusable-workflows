name: Reusable Workflow
run-name: Node ${{ inputs.node-version }} Quality Checks

on:
  workflow_call:
      inputs:
        node:
          required:  true
          type:  string

permissions:
  contents: read
